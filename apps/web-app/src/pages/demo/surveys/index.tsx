import React from 'react';
import SurveysList from '../../../components/surveys/SurveysList';

const SurveysIndexPage: React.FC = () => (
    <main>
      <SurveysList />
    </main>
  );

export default SurveysIndexPage;