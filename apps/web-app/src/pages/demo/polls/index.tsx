import React from 'react';
import PollsList from '../../../components/polls/PollsList';

const PollsIndexPage: React.FC = () => (
    <main>
      <PollsList />
    </main>
  );

export default PollsIndexPage;
